node_modules/
dist/
build/
# Excepción: íconos de la app empaquetada (binarios chicos, regenerables
# desde frontend/public/brand/eco-logo.svg con qlmanage + iconutil pero
# útil tenerlos committed para que `npm run dist:mac` funcione directo).
!electron/build/
!electron/build/icon.icns
!electron/build/icon.png
!electron/build/icon.ico
*.log
.env
.env.local
.DS_Store
*.tsbuildinfo

# Output de electron-builder — .dmg / .exe / .AppImage (cada uno ~150MB).
# Se regenera con `npm run dist:mac|win|linux`.
release/

# Python
__pycache__/
*.pyc
.venv/
venv/

# IDE
.vscode/
.idea/
*.swp

# Eco specific — binarios y modelos que se bajan al setup
data/eco.db
data/*.db
listener/models/
listener/.venv/
backend/piper/piper/
backend/piper/voices/*.onnx
backend/piper/*.tar.gz
